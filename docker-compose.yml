version: "3.8"

services:
  springboot-app:
    image: ajithak7353/springcicd:latest

    build:
      context: .
      dockerfile: Dockerfile
    container_name: springboot_app
    ports:
      - "9090:9090"
    restart: always
